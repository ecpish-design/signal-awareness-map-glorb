GLORB // SIGNAL MAPPER — FULL MOBILE SAFARI AUDIT

Replace only:
- index.html
- styles.css

Do not replace app.js, data.js, or any image assets.

The new responsive layer is scoped to screen widths of 680px and below.
Desktop, iPad, print and PDF export layouts remain unchanged.

After GitHub Pages deploys, close the existing Safari tab and reopen the URL in
a new tab so the audited stylesheet is loaded.
